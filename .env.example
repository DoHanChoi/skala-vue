# 이 파일을 .env 로 복사한 뒤 본인의 API 키를 채워 넣는다.
#   cp .env.example .env
#
# 키 발급: https://home.openweathermap.org/api_keys
# Vite는 VITE_ 접두사가 붙은 변수만 클라이언트 코드에 노출한다.
# 주의: 이 값은 빌드 결과물에 문자열로 포함되므로, 노출되면 곤란한 비밀키는 이 방식으로 다루면 안 된다.
VITE_OPENWEATHER_API_KEY=your_openweathermap_api_key_here
